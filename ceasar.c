#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
  if(argc == 2)
  {
    int k = atoi(arg [1]);
    char** str = GetString();
    
    return 0;
  }
  else
  {
    printf("You must enter one argument.");
    return 1;
  }
}
